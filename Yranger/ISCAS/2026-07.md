# 2026 年 08 月进展 - 实习生

实习生进展月度汇总

## Yranger

mentor : Hongbin Zhang

---

### 本月工作总结

#### 工作一:

* 实现了将 `linalg.generic` 操作转换为 `BOSCAME` 方言的转换过程；在 `LowerLinalgToBOSCAME.cpp` 中添加了核心降级逻辑，并更新了相应的构建配置与 makefile。
* 实现了 `linalg.generic` 的 `GenericMatmul`、`GenericElementwise`、`GenericTranspose`、`GenericBroadcast` 等转换操作，并编写了相关测试文件进行验证，编译生成的 `.elf` 文件在 `Gem5` 模拟器上测试结果均正确。

#### 工作二:

* 针对 `Qwen3` 模型切分导出并跑通了 `subgraph0_prefill_0_6b.mlir` 以及 `subgraph0_decode_0_6b.mlir` 两个阶段（Prefill 与 Decode 阶段）。
* 补充完善了 `RMSNorm` + `Softmax` 阶段所需的 `rsqrt` 算子操作；在 Buddy 端成功实现了归一化操作中的平方计算（逐元素浮点乘法）的转换与支持。
* **遇到问题**：在对归一化操作中的浮点平方计算进行测试时，发现 `Gem5` 模拟器暂不支持该浮点逐元素乘指令。

---

### 本月提交的PR

* [Lower linalg.generic to BOSCAME and add test cases by Yranger · Pull Request #829 · buddy-compiler/buddy-mlir](https://github.com/buddy-compiler/buddy-mlir/pull/829)

### 本月合并的PR

* 暂无
