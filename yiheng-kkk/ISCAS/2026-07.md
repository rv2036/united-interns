# 2026 年 07 月进展 - 实习生

实习生进展月度汇总

## 黄奕珩 (yiheng-kkk / Benji-code-beeo)

### mentor (Optional)

张洪滨

### 本月工作总结

本月主要围绕 `ruyi-serving` 的 `server_process` 诊断完善、Compute 模块职责拆分、OpenAI Chat Completions 参数兼容和 API 推理服务结构优化开展工作。6 月底提交的 `server_process` 故障诊断增强在 7 月合入 Upstream；随后完成诊断模块拆分与模型 modality 元数据方案、`max_completion_tokens` 兼容，以及 `inference_service.py` 按校验、prompt 构建和流式编排拆分。相关改动在 `buddy-gpuplct` 和 wafer 上使用真实 DeepSeek 与 Llama 3.1 模型完成验证。

### 本月提交的PR

- [RuyiAI-Stack/ruyi-serving#32: Modularize server process diagnostics and expose model modalities](https://github.com/RuyiAI-Stack/ruyi-serving/pull/32) - 未合入，已关闭；这是 #33 的临时 draft，包含相同的两项提交，调整提交分支后由 #33 接替。
- [RuyiAI-Stack/ruyi-serving#33: Modularize server process diagnostics and expose model modalities](https://github.com/RuyiAI-Stack/ruyi-serving/pull/33) - 未合入，已关闭；按 issue #31 将 child 输出捕获和结构化诊断从 `HttpChildProcessExecutor.cpp` 拆成独立组件，同时增加模型 modality 元数据及 API 能力透传。后续 Upstream 通过 PR #34 合入了 Compute 侧的模块化拆分，本 PR 因此关闭。
- [RuyiAI-Stack/ruyi-serving#35: fix(api): support max_completion_tokens](https://github.com/RuyiAI-Stack/ruyi-serving/pull/35) - 当前开放；在 Chat Completions HTTP 入口兼容 `max_completion_tokens`，保留旧的 `max_tokens`，两个字段同时出现时返回明确冲突错误，并让模型上限错误使用调用方实际传入的参数名。该改动不修改 protobuf、gRPC、Compute Server 和模型运行时。
- [RuyiAI-Stack/ruyi-serving#36: refactor(api): split inference service responsibilities](https://github.com/RuyiAI-Stack/ruyi-serving/pull/36) - 当前开放；将请求及模型策略校验移到 `inference_validation.py`，将 OpenClaw/Feishu 消息整理、上下文选择和 prompt 构建移到 `prompt_builder.py`，DeepSeek 与 Llama 3 模板统一保留在 domain 层。`inference_service.py` 继续负责 gRPC、SSE、错误映射、超时和断连取消，保持原有接口和行为不变。
- [rv2036/united-interns#105: Add yiheng-kkk June 2026 report](https://github.com/rv2036/united-interns/pull/105) - 未合入，已关闭；首次提交 2026 年 6 月 ISCAS 月报，因提交分支调整，由内容相同的 #106 接替。

### 本月合并的PR

- [RuyiAI-Stack/ruyi-serving#30: Add server_process runtime for persistent buddy-server](https://github.com/RuyiAI-Stack/ruyi-serving/pull/30) - 7 月 9 日合入；在已合入的 `server_process` 运行方式上补充 child stdout/stderr 有界 ring buffer、结构化失败分类、后台 reaper 和异常日志 tail，并覆盖非流式、SSE 流式、启动失败、请求超时、流空闲超时和子进程异常退出。
- [rv2036/united-interns#106: Add yiheng-kkk June 2026 report](https://github.com/rv2036/united-interns/pull/106) - 7 月 5 日合入；补交 2026 年 6 月 ISCAS 月报。

### 其他交付物

- [RuyiAI-Stack/ruyi-serving#29: Add log ring buffer function and classify exceptions](https://github.com/RuyiAI-Stack/ruyi-serving/issues/29) - 7 月完成实现说明和结项跟进，将 child 输出保存在有界 ring buffer 中，并以稳定的结构化字段区分启动失败、异常退出、OOM、请求超时和流空闲超时；对应实现随 PR #30 合入。
- [RuyiAI-Stack/ruyi-serving#31: Refactor HttpChildProcessExecutor.cpp](https://github.com/RuyiAI-Stack/ruyi-serving/issues/31) - 跟进 `server_process` 模块职责过多的问题，完成 child 输出捕获、诊断分类和测试边界梳理；对应的 Compute 侧模块化方案最终由 Upstream PR #34 合入。
- 针对 `inference_service.py` 的结构优化，复盘 GitHub issue、PR 和评论中关于参数语义、结构化错误和职责拆分的要求，并参考 vLLM、SGLang、TGI 对校验、prompt 和 streaming 的组织方式，形成保持现有行为的最小拆分方案。
- 为四种 prompt style 增加真实 JSONL 字节级 characterization tests，覆盖 OpenClaw/Feishu envelope、content parts、tool-call 历史、空 assistant placeholder、system workspace section、DeepSeek think 历史、多轮上下文截断和超长输入。
- 在 `buddy-gpuplct` 使用真实 DeepSeek 模型和 `huangyiheng_2026_02_real.jsonl` 完成基线与候选的非流式、流式、错误体、finish reason 和 trace 对照；在 wafer 使用真实 Llama 3.1 TT 模型完成 Python、CMake、CTest、real integration 和 benchmark 验证。
