# 2026 年 06 月进展 - 实习生

实习生进展月度汇总

## Michael-Chen-NJU

### Mentor: kiva

### 本月工作总结

本月在 LLVM 上游社区合入了四个 PR，工作主要集中在 VectorCombine、LoopVectorize 和 CodeGen 等方向，并持续围绕 RISC-V 相关场景补充修复与测试。具体包括：为 `foldShuffleChainsToReduce` 增加 FADD/FMUL 处理以增强向量优化覆盖；修复 ShrinkWrap 在 `FindIDom` 遇到空 predecessor/successor 列表时的崩溃；为 LoopVectorize 中宽指针 stride 场景补充 RISC-V 回归测试；以及在 size-optimized functions 下改进 VectorCombine 对 `TCK_CodeSize` 的使用。

### 本月合并的 PR

- [[VectorCombine] Use TCK_CodeSize for size-optimized functions](https://github.com/llvm/llvm-project/pull/202207) - 改进 size-optimized functions 下 VectorCombine 的成本模型选择，使其使用 `TCK_CodeSize`
- [[VectorCombine] foldShuffleChainsToReduce - add FADD/FMUL handling](https://github.com/llvm/llvm-project/pull/201302) - 为 `foldShuffleChainsToReduce` 增加 `FADD`/`FMUL` 处理，提升向量归约链折叠能力
- [[CodeGen] Fix ShrinkWrap crash when FindIDom receives empty predecessor/successor list](https://github.com/llvm/llvm-project/pull/198995) - 修复 ShrinkWrap 在 `FindIDom` 收到空 predecessor/successor 列表时的崩溃问题
- [[LV][RISCV] Add crash test for wide pointer stride in convertToStridedAccesses](https://github.com/llvm/llvm-project/pull/200985) - 为 `convertToStridedAccesses` 中 wide pointer stride 场景补充 RISC-V 崩溃回归测试
